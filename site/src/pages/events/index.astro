---
import PageLayout from '../../layouts/PageLayout.astro';
import { siteConfig } from '../../lib/siteConfig';
---

<PageLayout title="Events">
  <h1 class="text-3xl font-semibold text-[#12212b]">Events</h1>
  <p class="mt-3 text-[#3c4b57]">Upcoming events will be listed here.</p>

  <div class="mt-6 rounded-2xl border border-black/10 bg-white p-6 shadow-[0_16px_36px_rgba(15,23,42,0.08)]">
    <h2 class="text-xl font-semibold">Legacy Event Details</h2>
    <p class="mt-2 text-[#3c4b57]">
      For now, full event details are still hosted on the legacy site.
    </p>
    <a class="mt-4 inline-flex items-center gap-2 font-semibold text-[#2b5f47]" href={siteConfig.links.legacyEventDetails} target="_blank" rel="noopener noreferrer">
      Open event details on legacy site →
    </a>
  </div>
</PageLayout>
