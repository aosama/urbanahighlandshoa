---
import { siteConfig } from '../lib/siteConfig';

const basePath = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<footer class="mt-auto border-t border-slate-200 bg-white">
  <div class="mx-auto max-w-6xl px-6 py-12">
    <div class="grid gap-8 md:grid-cols-3">
      <!-- Brand -->
      <div class="flex flex-col gap-4">
        <div class="flex items-center gap-3">
          <img 
            src={`${basePath}assets/UH-logo.jpg`}
            alt="Urbana Highlands logo"
            class="h-10 w-10 rounded-xl shadow"
          />
          <span class="text-lg font-bold text-slate-900">Urbana Highlands</span>
        </div>
        <p class="text-sm text-slate-500">
          A serene community nestled in the rolling hills of Southern Frederick County, Maryland.
        </p>
      </div>

      <!-- Quick Links -->
      <div class="flex flex-col gap-4">
        <h3 class="text-sm font-semibold uppercase tracking-wider text-slate-400">Quick Links</h3>
        <div class="flex flex-col gap-2">
          <a class="text-sm text-slate-600 transition hover:text-emerald-600" href={siteConfig.links.residentPortal} target="_blank" rel="noopener noreferrer">Resident Portal</a>
          <a class="text-sm text-slate-600 transition hover:text-emerald-600" href={siteConfig.links.poolPass} target="_blank" rel="noopener noreferrer">Pool Pass Registration</a>
          <a class="text-sm text-slate-600 transition hover:text-emerald-600" href={siteConfig.links.facebook} target="_blank" rel="noopener noreferrer">Facebook Community</a>
        </div>
      </div>

      <!-- Contact -->
      <div class="flex flex-col gap-4">
        <h3 class="text-sm font-semibold uppercase tracking-wider text-slate-400">Contact</h3>
        <div class="flex flex-col gap-2 text-sm text-slate-600">
          <p>Management Company</p>
          <p class="font-semibold text-slate-900">{siteConfig.contact.managementPhone}</p>
        </div>
      </div>
    </div>

    <div class="mt-12 flex flex-col items-center justify-between gap-4 border-t border-slate-100 pt-8 md:flex-row">
      <p class="text-sm text-slate-400">
        © {new Date().getFullYear()} Urbana Highlands HOA. All rights reserved.
      </p>
      <div class="flex gap-4">
        <a 
          class="flex h-10 w-10 items-center justify-center rounded-full bg-slate-100 text-slate-500 transition hover:bg-emerald-100 hover:text-emerald-600"
          href={siteConfig.links.facebook} 
          target="_blank" 
          rel="noopener noreferrer"
          aria-label="Facebook"
        >
          <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24">
            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</footer>
